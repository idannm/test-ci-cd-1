services:
  jenkins:
    build: .
    container_name: jenkins
    user: root  # הוסף את השורה הזו כדי לעקוף בעיות הרשאות בשלב המעבדה
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - ./jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
